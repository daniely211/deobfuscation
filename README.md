# deobfuscation

To start the project:
Open 2 terminals
CD to the root of the repo:
In one terminal:
`cd backend`
`npm install`
`npm start`

In the other terminal:
`cd deobfuscate-app`
`npm install`
`npm start`


